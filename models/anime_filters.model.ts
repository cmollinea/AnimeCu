export type AnimeType = {
  default: string;
  movie: string;
  ova: string;
  special: string;
  ona: string;
};

export type AnimeStatus = {
  default: string;
  airing: string;
  upcoming: string;
};

export type AnimeOrderBy = {
  default: string;
  title: string;
  popularity: string;
  rank:string
};

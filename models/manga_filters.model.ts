export type MangaType = {
  default: string;
  novel: string;
  lightnovel: string;
  oneshot: string;
  doujin: string;
  manhwa: string;
  manhua: string;
};

export type MangaStatus = {
  default: string;
  complete: string;
  hiatus: string;
  discontinued: string;
};

export type MangaOrderBy = {
  default: string;
  mal_id: string;
  title: string;
  score: string;
  popularity: string;
};
